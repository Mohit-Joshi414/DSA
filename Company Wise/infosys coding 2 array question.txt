#include <bits/stdc++.h>

using namespace std;

int Solve(int n,vector<int> a){
   
    
}

int main()
{
    int n;
    cin>>n;
    
    vector<int> a(n);
    for(int j=0;j<n;j++){
        getline(cin,inputline);
        a[j] = =stoi(trim(inputline));
        
    }
    
    int result;
    result = Solve(n,a);
    cout<<result;

    return 0;
}
